# QA v3.3.3 — Sincronización backend

## Casos cubiertos
- Firebase Authentication autenticado y correo verificado.
- Lectura server-first `Config/app`.
- Lectura server-first `Usuarios/{uid}`.
- Consulta `Vigencias` con `isActive == true`.
- Fallback directo `Vigencias/{año actual}`.
- Fallback de colección completa con normalización del flag.
- Reintento manual sin recargar la página.
- Reintento automático al evento `online`.
- Estado superior Sincronizando / Sincronizado / Sin conexión / Error.
- El workspace no se bloquea en spinner infinito.

## Resultado esperado
Con `Vigencias/2026.isActive = true`, el selector superior debe mostrar `Vigencia 2026`.
Si `currentCycleId = null`, el sistema debe indicar `Sin corte publicado`, no un error de backend.
